package accessModifier;

public class AccessModifier {
    /**
     1-public
     2-protected (inheritance varsa baska packagelardan kullanılabilir)
     3-default==package private(hicbirsey yazmazsak default olur, aynı packagelardan kullanılabilir, farklı packagelardan inheritance olsa bile kullanılamaz)
     4-private

     public > protected > default > private

     Classlar private ve protected olamaz.

     */


}
